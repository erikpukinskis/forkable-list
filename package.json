{
  "name": "forkable-list",
  "version": "0.13.0",
  "description": "Build an array, fork it, and modify both branches, sharing memory between them",
  "main": "forkable-list.js",
  "dependencies": {
    "module-library": "*"
  },
  "devDependencies": {
    "run-test": "*"
  },
  "scripts": {
    "test": "node test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/erikpukinskis/forkable-list.git"
  }
}
